namePrefix: instance0-
namespace: rabbitmq
commonLabels:
  instance: instance0
resources:
  - service.yml
  - rbac.yml
  - statefulset.yml
  - configmap.yml

secretGenerator:
  - name: rabbitmq
    literals:
      # Generate using `printf "%s" "$(pwgen 20 1)" | base64`
      # format is:
      # - erlang-cookie=base64cookie
      - erlang-cookie=

