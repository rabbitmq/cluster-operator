apiVersion: v1
kind: Service
metadata:
  name: servicebroker
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: servicebroker
  sessionAffinity: None
  type: LoadBalancer
status:
  loadBalancer: {}
