---
apiVersion: v1
kind: ConfigMap
metadata:
  name: operator-config
  namespace: system
data:
  config: |
    service:
      type: NodePort
      annotations:
        service_annotations: 0.0.0.0/0
    resources:
      limits:
        memory: 1Gi
      requests:
        memory: 1Gi